#parse("CommonVars.vm")
## !LINKSTO Generator.Output.os_kconfig_h.Prologue, 1
/* This file is generated automatically. DO NOT EDIT!!
 * 
 * EB tresos AutoCore OS ${Version.getMajor()}.${Version.getMinor()}.${Version.getPatch()} ${Version.getTarget()}/${Version.getDerivate()}
 * (Build ${Version.getBuild()})
 * 
 * (c) 1998-${Version.getCurrentYear()} Elektrobit Automotive GmbH
 * Am Wolfsmantel 46
 * 91058 Erlangen
 * GERMANY
 * 
 * Date         : ${Version.getDate()}           !!!IGNORE-LINE!!!
 */

#ifndef OS_KCONFIG_H
\#define OS_KCONFIG_H
#ifdef __cplusplus
extern "C" {
#endif

## !LINKSTO Generator.Output.os_kconfig_h.Identification, 1
\#define OS_GENERATION_ID_OS_KCONF_H  $OsGenerator.getGenId()

\#define OS_AUTOSAROS_VER             $Version.getMajor()

\#define OS_AUTOSAROS_REV             $Version.getMinor()

\#define OS_AUTOSAROS_CORE_BUILD      $Version.getBuild()

\#define OS_AUTOSAROS_ARCH_BUILD      $Version.getBuild()

/*===================================================================
 * Configured Modes
 *==================================================================*/
/* The OS TRACER is configured as follows: */
#if( $OsConfig.hasTrace() )
#* *#\#define OS_USE_TRACE  1
#else
#* *#\#define OS_USE_TRACE  0
#end

/*===================================================================
 * Core Mapping
 *==================================================================*/
/* OS_INITCORE tells the build process which core the OS uses (singlecore). */
\#define OS_INITCORE  OS_U($OsConfig.getPhysicalInitCoreId())

/*===================================================================
 * Kernel Type
 *==================================================================*/
/* Defines which trapping mechanism the OS shall use. */
\#define OS_KERNEL_TYPE  OS_${OsConfig.getKernelType()}

/*===================================================================
 * Multicore
 *==================================================================*/
/* Defines the number of CPU cores assigned to EB tresos AutoCore OS. */
\#define OS_N_CORES  ${OsModel.getNumUsedCores()}
#ifdef __cplusplus
}
#endif
#endif  /* OS_KCONFIG_H */
